@model List<ResultEmployeeDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Admin/Index.cshtml";
    int count = 0;
}

<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-12">
            <div class="bg-light rounded h-100 p-4">
                <h6 class="mb-4">Danışman Listesi</h6>
                <td><a href="/Employee/CreateEmployee" class="btn btn-outline-info">Yeni Danışman</a></td>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Danışan Adı</th>
                                <th scope="col">Unvan</th>
                                <th scope="col">Mail</th>
                                <th scope="col">Telefon</th>
                                <th scope="col">Durum</th>
                                <th scope="col">Güncelle</th>
                                <th scope="col">Sil</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                count++;
                                <tr>
                                    <th scope="row">@count</th>
                                    <td>@item.Name</td>
                                    <td>@item.Title</td>
                                    <td>@item.Mail</td>
                                    <td>@item.Phone</td>
                                    <td>@item.Status</td>
                                    <td><a href="/Employee/UpdateEmployee/@item.ID" class="btn btn-outline-success">Güncelle</a></td>
                                    <td><a href="/Employee/DeleteEmployee/@item.ID" class="btn btn-outline-danger">Sil</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>